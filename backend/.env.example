# Backend — variables d'environnement (Ex. 3)
# Copier ce fichier en `.env` et adapter les identifiants.

# Port d'écoute de l'API REST
PORT=3000

# Base de données MySQL (H2 → MySQL via Prisma ; substitution documentée dans le worksheet)
DATABASE_URL="mysql://kanban:kanban@localhost:3306/jaydee_kanban"
