# Backend — variables d'environnement (Ex. 3)
# Copier ce fichier en `.env` et adapter si besoin.

# Port d'écoute de l'API REST
PORT=3000

# Fichier de base de données SQLite (H2 → SQLite, persistance sur disque)
DB_FILE=./data/kanban.sqlite
