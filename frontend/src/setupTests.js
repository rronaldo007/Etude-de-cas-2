// Configuration commune des tests frontend (chargée par Vitest — voir vite.config.js).
// Ajoute les matchers DOM de Testing Library (toBeInTheDocument, toHaveTextContent, ...).
import '@testing-library/jest-dom'
